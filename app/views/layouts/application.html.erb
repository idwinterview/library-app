<!DOCTYPE html>
<html>
	<head>
  <head>
    <title>LibraryApp</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

	<body>
			<%= render :partial => 'shared/header' if @show_header %>

			<div id="container">
				<%= render :partial => 'shared/side_nav' if @show_side_nav %>

	   		<%= yield %>

				<div class="clear"></div>
			</div>

		</body>
</html>
